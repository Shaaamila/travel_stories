import { Toast } from './toast';
export declare class MdToastComponent {
    toasts: Toast[];
    maxShown: number;
    /**
     * add toast
     * @param toast toast object with all parameters
     */
    add(toast: Toast): void;
    /**
     * remove toast
     * @param toastId number of toast id
     */
    remove(toastId: number): void;
    /**
     * check toast
     */
    isToast(): boolean;
}
